Reading
React lifecycle

1. Based off the diagram, what happens first, the ‘render’ or the ‘componentDidMount’?
Render
2. What is the very first thing to happen in the lifecycle of React?
Mounting
3. Put the following things in the order that they happen: componentDidMount, render, constructor, componentWillUnmount, React Updates
Constructor, Render, React Unpdates, componentDidMount, componentWillUnmount
4. What does componentDidMount do?
Invoked after a component is mounted, allowing React code to execute when the component is already in the DOM. 
Videos
React State Vs Props

1. What types of things can you pass in the props?
Any kind of information, such as titles, names, height, etc.
2. What is the big difference between props and state?
Props pass into a component, states are handled inside of a component. 
3. When do we re-render our application?
When the application is changed by the user.
4. What are some examples of things that we could store in state?
Updated values, or anything that needs to be handled inside the component. 
